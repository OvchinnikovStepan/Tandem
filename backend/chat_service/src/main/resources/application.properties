spring.application.name=chat_service

# ============ Server ============
server.port=8082

# ============ Database ============
spring.datasource.url=jdbc:postgresql://localhost:5432/ChatService
spring.datasource.username=postgres
spring.datasource.password=koroleva
spring.datasource.driver-class-name=org.postgresql.Driver

# ============ Flyway ============
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# ============ Kafka ============
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9093}

# --- Producer Configuration ---
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.acks=all
spring.kafka.producer.retries=3
spring.kafka.producer.enable-idempotence=true

# --- Consumer Configuration ---
spring.kafka.consumer.group-id=${spring.application.name}-local
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=com.tandem.*
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false

# --- Kafka Listener Configuration ---
spring.kafka.listener.ack-mode=manual
spring.kafka.listener.concurrency=3